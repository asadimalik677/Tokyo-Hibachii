const STORAGE_KEY="tokyoHibachiBookings";const SESSION_KEY="tokyoHibachiAdminLoggedIn";
// Change these two values to update the demo admin login credentials.
// For a public website, avoid using this static-only login for sensitive data.
const ADMIN_USERNAME="admin";const ADMIN_PASSWORD="ChangeMe123!";
const bookingForm=document.getElementById("bookingForm"),bookingMessage=document.getElementById("bookingMessage"),loginForm=document.getElementById("loginForm"),loginMessage=document.getElementById("loginMessage"),dashboard=document.getElementById("dashboard"),logoutButton=document.getElementById("logoutButton"),bookingList=document.getElementById("bookingList"),navToggle=document.querySelector(".nav-toggle"),navLinks=document.querySelector(".nav-links"),adminModal=document.getElementById("adminModal"),adminOpen=document.getElementById("adminOpen"),adminClose=document.getElementById("adminClose"),adminCloseButton=document.getElementById("adminCloseButton");
function getBookings(){return JSON.parse(localStorage.getItem(STORAGE_KEY)||"[]")}function saveBookings(bookings){localStorage.setItem(STORAGE_KEY,JSON.stringify(bookings))}function formatDateTime(dateValue,timeValue){if(!dateValue||!timeValue)return"Date/time not set";const date=new Date(`${dateValue}T${timeValue}`);return new Intl.DateTimeFormat("en-US",{month:"short",day:"numeric",year:"numeric",hour:"numeric",minute:"2-digit"}).format(date)}function createBooking(formData){return{id:crypto.randomUUID?crypto.randomUUID():String(Date.now()),fullName:formData.get("fullName").trim(),phone:formData.get("phone").trim(),email:formData.get("email").trim(),date:formData.get("date"),time:formData.get("time"),guests:Number(formData.get("guests")),request:formData.get("request").trim(),status:"Pending",createdAt:new Date().toISOString()}}
function updateDashboard(){const bookings=getBookings();const counts=bookings.reduce((totals,booking)=>{totals.total+=1;totals[booking.status.toLowerCase()]+=1;return totals},{total:0,pending:0,confirmed:0,completed:0});document.getElementById("totalBookings").textContent=counts.total;document.getElementById("pendingBookings").textContent=counts.pending;document.getElementById("confirmedBookings").textContent=counts.confirmed;document.getElementById("completedBookings").textContent=counts.completed;renderBookingList(bookings)}
function renderBookingList(bookings){if(!bookingList)return;if(bookings.length===0){bookingList.innerHTML='<div class="empty-state">No reservations yet. New online bookings will appear here.</div>';return}const sortedBookings=[...bookings].sort((a,b)=>new Date(`${a.date}T${a.time}`)-new Date(`${b.date}T${b.time}`));bookingList.innerHTML=sortedBookings.map(booking=>`<article class="booking-item"><div><h4>${escapeHtml(booking.fullName)} <span class="status-pill">${escapeHtml(booking.status)}</span></h4><div class="booking-meta"><span>${formatDateTime(booking.date,booking.time)}</span><span>${booking.guests} guest${booking.guests===1?"":"s"}</span><span>${escapeHtml(booking.phone)}</span><span>${escapeHtml(booking.email)}</span></div>${booking.request?`<p class="booking-request">${escapeHtml(booking.request)}</p>`:""}</div><div class="booking-actions"><select data-action="status" data-id="${booking.id}" aria-label="Change booking status">${["Pending","Confirmed","Completed"].map(status=>`<option value="${status}" ${booking.status===status?"selected":""}>${status}</option>`).join("")}</select><button class="btn delete-button" type="button" data-action="delete" data-id="${booking.id}">Delete</button></div></article>`).join("")}
function escapeHtml(value){return String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}function setAdminView(isLoggedIn){sessionStorage.setItem(SESSION_KEY,isLoggedIn?"true":"false");loginForm.classList.toggle("is-hidden",isLoggedIn);dashboard.classList.toggle("is-hidden",!isLoggedIn);if(isLoggedIn)updateDashboard()}function openAdminModal(){adminModal.classList.remove("is-hidden");adminModal.setAttribute("aria-hidden","false")}function closeAdminModal(){adminModal.classList.add("is-hidden");adminModal.setAttribute("aria-hidden","true")}
bookingForm.addEventListener("submit",event=>{event.preventDefault();const formData=new FormData(bookingForm),booking=createBooking(formData),bookings=getBookings();bookings.push(booking);saveBookings(bookings);bookingForm.reset();bookingMessage.textContent="Thank you. Your reservation has been received and is pending confirmation.";if(sessionStorage.getItem(SESSION_KEY)==="true")updateDashboard()});
loginForm.addEventListener("submit",event=>{event.preventDefault();const formData=new FormData(loginForm),username=formData.get("username"),password=formData.get("password");if(username===ADMIN_USERNAME&&password===ADMIN_PASSWORD){loginMessage.textContent="";loginForm.reset();setAdminView(true)}else loginMessage.textContent="Incorrect username or password."});logoutButton.addEventListener("click",()=>setAdminView(false));bookingList.addEventListener("change",event=>{if(event.target.dataset.action!=="status")return;const bookings=getBookings().map(booking=>booking.id===event.target.dataset.id?{...booking,status:event.target.value}:booking);saveBookings(bookings);updateDashboard()});bookingList.addEventListener("click",event=>{if(event.target.dataset.action!=="delete")return;const bookings=getBookings().filter(booking=>booking.id!==event.target.dataset.id);saveBookings(bookings);updateDashboard()});navToggle.addEventListener("click",()=>{const isOpen=navLinks.classList.toggle("is-open");navToggle.setAttribute("aria-expanded",String(isOpen))});navLinks.addEventListener("click",()=>{navLinks.classList.remove("is-open");navToggle.setAttribute("aria-expanded","false")});adminOpen.addEventListener("click",openAdminModal);adminClose.addEventListener("click",closeAdminModal);adminCloseButton.addEventListener("click",closeAdminModal);document.getElementById("year").textContent=new Date().getFullYear();setAdminView(sessionStorage.getItem(SESSION_KEY)==="true");